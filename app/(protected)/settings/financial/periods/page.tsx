'use client';
import { useState, useEffect } from 'react';
import { supabase } from '@/lib/supabase';
export default function PeriodsPage() {
  const [periods, setPeriods] = useState<any[]>([]);
  useEffect(() => { async function load() { const { data: { session } } = await supabase.auth.getSession(); if (!session) return; const { data: entities } = await supabase.rpc('auth_entities'); if (!entities?.length) return; const { data } = await supabase.from('financial_periods').select('*').eq('entity_id', entities[0]).order('period_start', { ascending: false }).limit(20); setPeriods(data || []); } load(); }, []);
  return (<div className="space-y-6 max-w-4xl"><h1 className="text-2xl font-light tracking-[-0.02em] text-white">Financial Periods</h1><div className="rounded-xl border border-white/[0.06] overflow-hidden"><table className="w-full text-sm"><thead><tr className="border-b border-white/[0.06] bg-white/[0.02]"><th className="text-left py-3 px-4 text-[11px] font-medium text-zinc-500 uppercase">Period</th><th className="text-left py-3 px-4 text-[11px] font-medium text-zinc-500 uppercase">Type</th><th className="text-left py-3 px-4 text-[11px] font-medium text-zinc-500 uppercase">Start</th><th className="text-left py-3 px-4 text-[11px] font-medium text-zinc-500 uppercase">End</th><th className="text-center py-3 px-4 text-[11px] font-medium text-zinc-500 uppercase">Status</th></tr></thead><tbody>{periods.map(p => (<tr key={p.id} className="border-b border-white/[0.03]"><td className="py-2.5 px-4 text-white font-light">{p.period_name}</td><td className="py-2.5 px-4 text-zinc-400 capitalize">{p.period_type}</td><td className="py-2.5 px-4 text-zinc-400 text-xs">{p.period_start?.split('T')[0]}</td><td className="py-2.5 px-4 text-zinc-400 text-xs">{p.period_end?.split('T')[0]}</td><td className="py-2.5 px-4 text-center"><span className={`text-[10px] px-2 py-0.5 rounded-full ${p.status === 'open' ? 'bg-emerald-500/10 text-emerald-400' : 'bg-zinc-800 text-zinc-500'}`}>{p.status}</span></td></tr>))}</tbody></table></div></div>); }
